{
  "name": "edgesoft-math-utils",
  "version": "1.0.0",
  "description": "Mathematical utility functions with TDD implementation",
  "main": "src/mathUtils.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "src/**/*.js"
    ]
  }
}